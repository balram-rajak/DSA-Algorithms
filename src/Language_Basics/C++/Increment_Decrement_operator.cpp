#include<bits/stdc++.h>
using namespace std;
int main(int argc, char const *argv[])
{
    int x=2;
// used before increment
    cout<<x++;
// used after increment
    cout<<++x;
// used before decrement
    cout<<x--;
// decrement before use
    cout<<--x;

    return 0;
}
